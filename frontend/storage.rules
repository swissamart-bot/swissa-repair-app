rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Repair images — authenticated clients only (Anonymous Auth OK).
    // Do NOT use: allow read, write: if true;
    match /repair-jobs/{jobId}/items/{itemId}/{fileName} {
      allow read, write: if request.auth != null;
    }
  }
}
